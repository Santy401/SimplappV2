# Force a single shared instance of @prisma/client across the monorepo.
# This is required because Prisma generates types at build time and
# pnpm's strict isolation would otherwise create multiple disconnected instances.
shamefully-hoist=true
